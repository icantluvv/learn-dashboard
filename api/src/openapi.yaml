openapi: 3.0.2

servers:
  - url: "https://example.com"

info:
  version: 1.0.0
  title: Example
  license:
    name: Proprietary
    url: https://exmaple.com

tags:
  - name: Registration
    description: Registration operations
  - name: Me
    description: Current user
  - name: Skills
    description: Skill catalog operations
paths:
  /api/example:
    $ref: paths/api_example.yaml
  /api/me:
    $ref: paths/api_me.yaml
  /api/skills:
    $ref: paths/api_skills.yaml
  /api/skills/{id}:
    $ref: paths/api_skills_id.yaml

components:
  securitySchemes:
    auth_cookie:
      $ref: components/securitySchemes/auth_cookie.yaml
  schemas:
    AuthMe:
      $ref: components/schemas/AuthMe.yaml
    Skill:
      $ref: components/schemas/Skill.yaml
