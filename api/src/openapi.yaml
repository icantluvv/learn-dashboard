openapi: 3.0.2

servers:
  - url: "https://example.com"

info:
  version: 1.0.0
  title: Example
  license:
    name: Proprietary
    url: https://exmaple.com

tags:
  - name: Registration
    description: Registration operations
  - name: Me
    description: Current user
paths:
  /api/example:
    $ref: paths/api_example.yaml

components:
  securitySchemes:
    auth_cookie:
      $ref: components/securitySchemes/auth_cookie.yaml
